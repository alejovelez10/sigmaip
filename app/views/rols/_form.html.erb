<div id="contact-modal" class="modal fade">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Cerrar</span></button>
        <h4 class="modal-title">Rol</h4>
      </div>

         <div class="modal-body">




<%= form_with(model: rol, local: true) do |form| %>
  <% if rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:</h2>

      <ul>
      <% rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
<div class="row">
<div class="col-md-12">
   <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :rol_name %>
  </div>

</div>

<div class="col-md-6" style="margin-top:20px">
    <div class="field">
    <%= form.label "Administrar Pagina", style:"margin-right:20px" %>
    <%= form.check_box :admin_page, id: "toggle-1", class: "ck" %>
  </div>

</div>

<div class="col-md-6" style="margin-top:20px">
    <div class="field">
    <%= form.label "Administrar Documentos", style:"margin-right:20px"%>
    <%= form.check_box :admin_doc, id: :rol_admin_doc, class: "ck"%>
  </div>
</div>


<div class="col-md-12" style="margin-top:20px">
    <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :rol_descripcion %>
  </div>

</div>
  
</div>




<div class="modal-footer text-center">
       
        <%= form.submit "Guardar", class: 'btn btn-primary btnpad' %>
         <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
      </div>
      <%end%>
</div>
  </div>
</div>
</div>
